{
  "mcp_server_config": {
    "name": "styxy",
    "description": "Smart port management for Claude Code through styxy daemon",
    "version": "1.0.0",
    "server_command": {
      "command": "node",
      "args": ["/home/cordlesssteve/mcp-servers/styxy-server/src/index.js"]
    }
  },
  "required_permissions": [
    "mcp__styxy__styxy_allocate_port",
    "mcp__styxy__styxy_status",
    "mcp__styxy__styxy_logs",
    "mcp__styxy__styxy_config",
    "mcp__styxy__styxy_cleanup",
    "mcp__styxy__styxy_metrics"
  ],
  "installation_instructions": {
    "step_1": "Add server config to ~/.claude/mcp.json",
    "step_2": "Add permissions to project .settings.local.json",
    "step_3": "Ensure styxy daemon is running: styxy daemon start",
    "step_4": "Test with: /mcp styxy_status"
  },
  "dependencies": {
    "styxy_daemon": "Required - must be running for full functionality",
    "styxy_cli": "Required - used as fallback when daemon unavailable",
    "node_version": ">=16.0.0"
  }
}